/**
 * Checks if the user is authenticated.
 *
 * @param {object} user - The user object.
 * @returns {boolean} - True if the user is null, false otherwise.
 */
export const handleAuth = (user) => user === null;
