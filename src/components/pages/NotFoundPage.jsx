/**
 * Represents the NotFoundPage component.
 * Renders a message and heading when a page is not found.
 *
 * @returns {JSX.Element} The rendered component.
 */
const NotFoundPage = () => {
  return (
    <div>
      <h1>NotFoundPage</h1>
    </div>
  );
};

export default NotFoundPage;
