import { Spinner } from "react-bootstrap";

/**
 * A function that returns a spinner component.
 *
 * @function spinner
 * @returns {JSX.Element} - The spinner component.
 */
export const spinner = () => <Spinner animation="border" variant="primary" />;
