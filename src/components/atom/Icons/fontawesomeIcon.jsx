/**
 * Function to render a Font Awesome icon.
 * @param {string} iconClassName - The class name of the desired font icon.
 * @returns {JSX.Element} The Font Awesome icon component.
 */
export const fontawesomeIcon = (iconClassName) => (
  <i className={`${iconClassName}`}></i>
);
